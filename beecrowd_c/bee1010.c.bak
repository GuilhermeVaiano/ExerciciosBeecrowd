# include <stdio.h>
/*
Neste problema, deve-se ler o código de uma peça 1, 
o número de peças 1, o valor unitário de cada peça 1, 
o código de uma peça 2, o número de peças 2 e o valor
unitário de cada peça 2. Após, calcule e mostre o valor 
a ser pago. O arquivo de entrada contém duas linhas 
de dados. Em cada linha haverá 3 valores, respectivamente 
dois inteiros e um valor com 2 casas decimais.

*/

int main (void) {
	int cod1,qtd1,cod2,qtd2;
	double preco1,preco2;
	scanf("%d %d %lf",&cod1,&qtd1,&preco1);
	scanf("%d %d %lf",&cod2,&qtd2,&preco2);
	printf("VALOR A PAGAR: R$ %.2f\n", (qtd1*preco1) + (qtd2*preco2));
	return 0;
}
